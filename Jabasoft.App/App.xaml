<Application x:Class="Jabasoft.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Jabasoft.App"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Shared typography/styles every real app merges in -
                     see Stylebook.Components/Styles/Theme.xaml. -->
                <ResourceDictionary Source="pack://application:,,,/Stylebook.Components;component/Styles/Theme.xaml" />
                <!-- Jabasoft v1 has no database of its own, so no
                     DB-driven live theme (unlike Stylebook.Playground) -
                     just the static Visual Studio palette, same
                     "fixed look, always" choice Stylebook.Playground's
                     own chrome makes. -->
                <ResourceDictionary Source="pack://application:,,,/Stylebook.Components;component/Styles/Themes/VisualStudio.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
