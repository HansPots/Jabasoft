<UserControl x:Class="Jabasoft.App.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Nog lege placeholder - komt later terug als een echt gestyled
         component uit de Stylebook (zie MainWindow.xaml.cs's
         BuildActionContent). -->
    <TextBox Text="Instellingen - nog niets hier."
             Style="{StaticResource SelectableTextStyle}"
             Foreground="{DynamicResource TextMutedBrush}"
             HorizontalAlignment="Center"
             VerticalAlignment="Center" />
</UserControl>
